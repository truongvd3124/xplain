fastapi>=0.110
uvicorn[standard]>=0.29
pydantic-settings>=2.0
python-multipart>=0.0.9
Pillow>=10.0
numpy>=1.24
# CLIP + torch are already provided by the parent zcbm venv.
# AI concept extraction (optional -- only needed if GEMINI_API_KEY is set):
google-genai>=0.3
