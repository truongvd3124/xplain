name: MVP CI & Docker Build

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

permissions:
  contents: read
  packages: write  # Quyền push Docker image lên GitHub Packages (GHCR)

jobs:
  frontend-check:
    name: Frontend Build Check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: xplaincv-mvp/frontend/package-lock.json

      - name: Install dependencies
        working-directory: xplaincv-mvp/frontend
        run: npm ci

      - name: Build frontend
        working-directory: xplaincv-mvp/frontend
        run: npm run build

  backend-check:
    name: Backend Syntax Check
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
          cache-dependency-path: xplaincv-mvp/backend/requirements.txt

      - name: Install dependencies
        working-directory: xplaincv-mvp/backend
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Lint check
        working-directory: xplaincv-mvp/backend
        run: |
          python -m pip install flake8
          flake8 . --count --select=E9,F63,F7,F82 --show-source --statistics

  docker-build:
    name: Build & Push Docker Images
    runs-on: ubuntu-latest
    needs: [frontend-check, backend-check]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # Đăng nhập vào GitHub Container Registry (GHCR) bằng token tự động của workflow
      - name: Log in to GitHub Container Registry
        if: github.event_name == 'push'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Chuyển đổi tên repo thành dạng chữ thường (lowercase) để hợp lệ với Docker tag
      - name: Lowercase repository name
        id: repo-name
        run: echo "repo=${GITHUB_REPOSITORY,,}" >> $GITHUB_OUTPUT

      # Build & Push API Image
      - name: Build and push API Docker image
        uses: docker/build-push-action@v5
        with:
          context: xplaincv-mvp/backend
          file: xplaincv-mvp/backend/Dockerfile
          push: ${{ github.event_name == 'push' }}
          tags: |
            ghcr.io/${{ steps.repo-name.outputs.repo }}/mvp-api:latest
            ghcr.io/${{ steps.repo-name.outputs.repo }}/mvp-api:${{ github.sha }}

      # Build & Push Frontend Image
      - name: Build and push Frontend Docker image
        uses: docker/build-push-action@v5
        with:
          context: xplaincv-mvp/frontend
          file: xplaincv-mvp/frontend/Dockerfile
          push: ${{ github.event_name == 'push' }}
          tags: |
            ghcr.io/${{ steps.repo-name.outputs.repo }}/mvp-frontend:latest
            ghcr.io/${{ steps.repo-name.outputs.repo }}/mvp-frontend:${{ github.sha }}
